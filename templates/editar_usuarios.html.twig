{% extends 'base.html.twig' %}

{% block title %}Editar Clientes{% endblock %}

{% block body %}

    <section class="jumbotron p-1 text-center">
            <div class="container">
            <h1 class="jumbotron-heading">Editar Usuarios</h1>
            <p class="lead text-muted">Editar los usuarios</p>
            </div>
    </section>

   <main>
    <div class="container w-60">

        
        {{ form_start(formulario) }} 
        {{ form_widget(formulario) }} 
        {{ form_end(formulario) }}


        {# BOTON ATRAS #}
        <div class="container">
            <div class="nav justify-content-end">
                <a class="btn btn-primary"  href="{{ path('usuarios')}}" >Atrás</a>
            </div>
        </div>
    </div>
<main>

</div>

{% endblock %}